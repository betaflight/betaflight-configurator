{
    "$schema": "../gen/schemas/iOS-schema.json",
    "identifier": "ios",
    "description": "Temporary local test capability: core APIs and BLE on iOS.",
    "platforms": ["iOS"],
    "windows": ["*"],
    "permissions": [
        "core:default",
        "shell:allow-open",
        "blec:allow-check-permissions"
    ]
}
